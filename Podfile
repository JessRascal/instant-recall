# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'instant-recall' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for instant-recall
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'

  target 'instant-recallTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'instant-recallUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
